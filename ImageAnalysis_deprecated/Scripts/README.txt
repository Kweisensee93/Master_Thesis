Shiny UI sliders / dropdowns
        ↓
Update Parameters button
        ↓
R writes YAML
        ↓
Check button
        ↓
R calls Python script
        ↓
Python reads YAML → runs pipeline → saves images
        ↓
Shiny displays images
